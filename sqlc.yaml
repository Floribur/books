version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/schema_sqlite.sql"
    queries: "sql/queries/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_pointers_for_null_types: true
